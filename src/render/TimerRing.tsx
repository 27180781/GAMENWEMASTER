/** מצב הטיימר (SPEC סעיף 9) — נותר/סה"כ ומצב עצירה. התצוגה עצמה מרונדרת
 *  בתוך QuestionSlide (פס q-countdown); כאן רק הטיפוס המשותף. */

export interface TimerView {
  remaining: number;
  total: number;
  /** הטיימר עצור בפקודת מנחה (6) — ההצבעה קפואה. */
  paused: boolean;
  /**
   * הזמן האפקטיבי שחלף מפתיחת ההצבעה (ms, בלי משך העצירות) ברגע הדגימה —
   * אותו שעון שלפיו המנוע מנקד את הניקוד היורד, כדי שהמספר שעל המסך הוא
   * המספר שמקבלים.
   */
  elapsedMs: number;
  /** ‎Date.now()‎ ברגע הדגימה — תצוגת הניקוד היורד ממשיכה ממנו חלק (rAF) עד הדגימה הבאה. */
  sampledAt: number;
}
