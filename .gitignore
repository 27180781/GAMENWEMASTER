node_modules
dist
release
*.local
.DS_Store
coverage

# TypeScript incremental build cache
*.tsbuildinfo

# הקלטות ביניים של סקריפט המדריך (ראו tools/guide/harness.mjs)
/public/guide/.raw/
# הקלטות מריצות אימות אד-הוק (slug שמתחיל ב-check-). פרקי המדריך עצמם
# (01-…06-) כן נשמרים בגיט. בלי זה כל בדיקה בדפדפן משאירה וידאו שנכנס גם
# ל-dist וגם ל-EXE שהלקוח מוריד.
/public/guide/check-*.webm
